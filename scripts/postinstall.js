// Postinstall script for chromium-min
// chromium-min downloads Chromium automatically at runtime, so this script is optional
// It can be used to pre-warm or verify the setup
console.log('✅ Postinstall complete - chromium-min will download Chromium at runtime');

